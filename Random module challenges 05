import random

num = random.randint(1, 10)

correct = False

while correct == False:
  guess = int(input("Guess a number between 1 and 10! >>>"))
  
  if guess == num:
    correct = True
  else:
    print("Try again!")
